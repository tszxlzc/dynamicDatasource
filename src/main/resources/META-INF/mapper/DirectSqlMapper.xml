<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="liuzc.dynamicDatasource.dal.mapper.DirectSqlMapper">

    <select id="getListBySql" parameterType="string" resultType="map">
        ${sql}
    </select>

    <select id="getObjectBySql" parameterType="string" resultType="object">
        ${sql}
    </select>
</mapper>